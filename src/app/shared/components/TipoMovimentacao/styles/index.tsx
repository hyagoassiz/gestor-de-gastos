import { Box } from "@mui/material";
import { styled } from "@mui/system";

export const StyledBox = styled(Box)(() => ({
  display: "flex",
  alignItems: "center",
  width: "90px",
  borderRadius: "10px",
}));
